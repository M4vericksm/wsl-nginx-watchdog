#!/bin/bash

# Esse script é opcional, caso deseje utiliza-lo apenas elimine as hashtags ;)

#NGINX_SERVICE="nginx"

# Verifica se o NGINX está offline
#if ! systemctl is-active --quiet $NGINX_SERVICE; then
#    echo "NGINX está offline. Iniciando o serviço..."
#    sudo systemctl start $NGINX_SERVICE

    # Verifica se o NGINX foi iniciado com sucesso
#    if systemctl is-active --quiet $NGINX_SERVICE; then
#        echo "NGINX iniciado com sucesso."
#    else
#        echo "Falha ao iniciar o NGINX."
#        exit 1
#    fi
#else
#   echo "NGINX já está online."
#fi

